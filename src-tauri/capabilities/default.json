{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "settings", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["settings", "overlay", "radial"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default",
    "dialog:default"
  ]
}
